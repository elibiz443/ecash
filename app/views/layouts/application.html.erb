<!DOCTYPE html>
<html lang="en">
  <head>
    <title>eCASH</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Web Fonts -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Rubik:300,300i,400,400i,500,500i,700,700i,900,900i">

    <!-- Stylesheet -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css" />

    <link rel="stylesheet" href="/assets/css/style.css">
    <%= favicon_link_tag asset_path('favicon.png') %>
  </head>
  <body>
    <div id="main-wrapper">
      <% if controller_name == "dashboards" || controller_name == "sends" || controller_name == "success" %>
        <%= render 'shared/dashheader' %>
      <% end %>

      <% unless controller_name == "dashboards" || controller_name == "sends" || controller_name == "success" %>
        <%= render 'shared/header' %>
      <% end %>
        <%= yield %>
      <% if controller_name == "dashboards" || controller_name == "sends" || controller_name == "success" || controller_name == "sessions" || controller_name == "users" %>
        <%= render 'shared/dashfooter' %>
      <% end %>

      <% unless controller_name == "dashboards" || controller_name == "sends" || controller_name == "success" || controller_name == "sessions" || controller_name == "users" %>
        <%= render 'shared/footer' %>
      <% end %>
      <%= javascript_pack_tag 'main', 'data-turbolinks-track': 'reload' %>
    </div>
  </body>
</html>
